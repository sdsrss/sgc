node_modules/
dist/
.tmp/
*.log
.env
.env.*
!.env.example
.DS_Store
Thumbs.db

# Browse binary (built from source)
plugins/sgc/browse/dist/

# Runtime state (project-specific, not plugin code)
.sgc/
.context/

# IDE / editor / Claude session metadata
.claude/

# Git worktrees (isolated feature branches)
.worktrees/

# bun.lock not used — project lockfile is package-lock.json (npm).
# Bun is used as runtime (bun run, bun test, bun build) but reads package-lock.json fine.
bun.lock
