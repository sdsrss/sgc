# Scope `bun test` discovery to sgc's own gate (dispatcher + eval).
#
# CI runs `bun test tests/dispatcher [tests/eval]` explicitly; this makes a bare
# local `bun test` (no path) match that scope — discovering only `tests/` rather
# than scanning the whole repo.
[test]
root = "tests"
